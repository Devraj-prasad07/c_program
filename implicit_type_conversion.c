#include <stdio.h>
int main()
{
    float a, b;
    int sum;
    printf("Enter the value of A: ");
    scanf("%f", &a);
    printf("Enter the value of B: ");
    scanf("%f", &b);
    sum = a + b;
    printf("The Result is: %d",sum);
    return 0;
}